export const DISPLAY_CONFIG = {
  // X position where the second monitor starts
  // = the width of the primary (laptop) screen in pixels
  // Common values: 1366 (HD laptop), 1920 (FHD laptop), 2560 (2K laptop)
  secondScreenX: 1920,

  // Y position (usually 0 unless monitors are offset vertically)
  secondScreenY: 0,

  // Resolution of the second/larger display
  // Update these to match your actual display on event day
  displayWidth: 1920,
  displayHeight: 1080,
};